import 'package:flutter/material.dart';

class EditContainer extends StatelessWidget {
  const EditContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Düzenleme Ekranı'),
    );
  }
}
